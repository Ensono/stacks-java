spring:
  application:
    name: xxSTACKSxx-api
  data:
    rest:
      detection-strategy: annotated

# The basepath, passed in as `API_BASEPATH` by Kubernetes.
api:
  basepath: ''

server:
  forward-headers-strategy: framework
  port: 9000
  # servlet:
  #   context-path: ${api.basepath}

management:
  endpoints:
    web:
      base-path: /

springdoc:
  # api-docs:
  #   path: /api-docs
  swagger-ui:
    disable-swagger-default-url: true
    path: /swagger/index.html
    # urls:
    #   - url: ${api.basepath}/api-docs
    #     name: Api Docs
  packagesToScan: com.xxAMIDOxx.xxSTACKSxx.menu.api.v1

azure:
  cosmosdb:
    uri: https://amido-stacks-dev-eun-java-api.documents.azure.com:443/
    database: Stacks
    key: xxxxxx
  application-insights:
    instrumentation-key: xxxxxx
    enabled: true
  keyvault:
    enabled: false
    uri: https://amido-stacks-tmp.vault.azure.net/
    client-id: xxxxxx
    client-key: xxxxxx
    tenant-id: xxxxxx
  servicebus:
    connectionString: xxxxxx
    topicName: menu-updates
    subscriptionName: Subscription1
    enabled: false
